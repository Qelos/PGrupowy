@model IEnumerable<Zaliczenie.Models.KolejnyMecz>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Gospodarz)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gość)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model =>item.Gospodarz)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gość)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Gospodarz">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Gospodarz">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Gospodarz">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
